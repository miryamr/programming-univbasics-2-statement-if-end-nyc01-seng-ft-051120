# Write your solution here
current_time = Time.now.even?
puts "Even!"
if current_time = true
else
  puts "Odd!"
end 


#Assign `run_code_inside`
#. Print out with `puts "Code before if...end"`
#3. Reach the `if` statement
#. Evaluate the expression to the right of the `if` statement
# Since the expression `run_code_inside` evaluates `true`, we perform the code
   #between the `if` and `end` reserved words
#Print out with `puts "Code after if...end"`
